class Airport {
	constructor(name) {
		this.name = name
		this.planes = this.planes //what would be the best datatype?
	}

	addPlane(plane) {
		//use an array method
		console.log("Get on plane...")
	}
}

module.exports = Airport